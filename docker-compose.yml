version: '3'
services:
  filebeat:
    build: ./filebeat
    depends_on:
      - logstash
  logstash:
    build: ./logstash
    depends_on:
      - elasticsearch
  elasticsearch:
    build: ./elasticsearch
    ports:
      - "9200:9200"